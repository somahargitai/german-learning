import { Divider } from '@mui/material';

const GreyLine = () => {
  return (
    <Divider
      sx={{
        width: '100%',
        margin: '0px',
        color: 'black',
      }}
    />
  );
};

export default GreyLine;
